# Copy this file outside version control and replace every placeholder before
# running the Send Email Hook locally. Never commit the real values.
SEND_EMAIL_HOOK_SECRET=v1,whsec_REPLACE_WITH_BASE64_SECRET
TWILIO_API_KEY_SID=SK_REPLACE_ME
TWILIO_API_KEY_SECRET=REPLACE_ME
